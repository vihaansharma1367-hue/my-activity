name: My Activity Bot

on:
  # این بخش زمان‌بندی است (کرون جاب)
  schedule:
    - cron: '0 9 * * *'  # هر روز ساعت ۹ صبح به وقت جهانی اجرا می‌شود
  
  # این خط اجازه می‌دهد دکمه Run را دستی هم بزنید (برای تست)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  auto-commit:
    runs-on: ubuntu-latest
    steps:
      # مرحله ۱: دریافت کدهای ریپازیتوری
      - name: Checkout code
        uses: actions/checkout@v3

      # مرحله ۲: انجام تغییر (اضافه کردن تاریخ امروز به فایل)
      - name: Update Log File
        run: |
          echo "Activity Log: $(date)" >> activity_log.txt

      # مرحله ۳: ثبت تغییرات و ارسال به گیت‌هاب
      - name: Commit and Push
        run: |
          git config user.name "My Activity Bot"
          git config user.email "actions@github.com"
          git add activity_log.txt
          git commit -m "🤖 Bot Activity: Updated log for $(date +'%Y-%m-%d')"
          git push
